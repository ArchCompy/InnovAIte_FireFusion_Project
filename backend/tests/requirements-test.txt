pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27

# The unit tests import the real ForecastService and MisinformationService,
# so their runtime dependencies must be present or every unit test silently skips.
redis>=5.0
fastapi>=0.115
pydantic-settings>=2.0
aio_pika>=9.0
psycopg[binary,pool]>=3.1

# test_tracing.py imports the real shared/tracing.py, and forecast_service.py
# now imports it too, so these must be present or those tests silently skip.
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-grpc
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-logging